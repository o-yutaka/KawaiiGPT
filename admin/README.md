# NEAR Admin

The admin surface is a separate privileged application. It must never ship service-role credentials to the mobile client.

Initial modules:
- moderation queue
- user reports
- blocks/reports audit
- city density metrics
- feature flags
- revenue/affiliate telemetry
- account deletion/export requests
