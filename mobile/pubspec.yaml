name: near
publish_to: "none"
description: NEAR Local Real-Time Social World prototype.
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
